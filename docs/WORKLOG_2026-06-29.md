# WORKLOG — 2026-06-29

> 하루 작업 요약. 상세 변경 이력은 `docs/PLAN_CHANGELOG.md`, 처리 흐름 예시는 `docs/QUERY_TRACE_EXAMPLE.md` 참조.

## 1. 게이트웨이 실연결 & 모델 교체
- 사내망에서 게이트웨이 3종 실연결 검증: Octen 임베딩(4096d), HCX chat, Qwen3.6 — **3/3 통과**.
- Qwen 응답 파서 보강(reasoning 모델 `reasoning_content` 대응).
- **HCX 모델 교체**: HyperCLOVA X SEED-32B → **HCX-30B-Text(`hcx-agent-05`, 223.130.140.68:11000)**.
  - config/.env/.env.example/docs/presentation 전반 반영. chat=`HCX30_*`, 임베딩키 분리(`EMBEDDING_API_KEY`, Octen은 namc-aigw 유지).

## 2. 인덱싱·저장 확장
- 실제 Octen 4096d로 재인덱싱, **Qdrant `woori_auto` 252점 적재** 확인.
- 임베딩 배치화 + 진행률 출력(순차 252회 → 8배치), 배치 실패 시 개별 실호출 폴백.
- Qdrant 적재 경로(`app/qdrant_io.py`) + 차원 자동결정(폴백 256d/실제 4096d 혼입 방지) + 재인덱싱 원스톱(`scripts/reindex.py`).

## 3. tables.db 커버리지 대폭 확장 (source 전 문서 스캔)
- 타 상품: 중형트럭(상품조건 20), 재고금융(5), 중고리스 잔가군(70), 법인 적용금리.
- 수치·규칙 누락분 일괄 반영: 연체이자율·중도상환수수료율·슬라이딩·판촉수수료·네고상한(중고승용), 금융리스금리·IRR NEGO·취급기간(중고리스), 신청가능금액·최대대출(DB시세 110%)·대상물품 연식·카히스토리(심사), 특별승인·책임채권·임직원금리(FAQ).
- **정확도 버그 수정**: NICE 점수→등급표를 원문 실제값으로 교체(1=933↑…8=610↑,9=350↑). 부작용으로 초기 예시 'NICE 885=1등급'은 실제 2등급으로 정정(원문 기준) — 도메인 확인 요청 상태.
- 최종: rate_grade 45 / nego_rule 36 / condition_rule 51 / lease_residual_group 70 / nice_band 9.

## 4. 조회 로직 고도화 (환각·오매칭 제거)
- 신규 핸들러: 잔가군, 프로모션(+가능/불가 **판정**), 법인, 네고 개요, 팩트 키워드 라우터.
- 오매칭 수정: 상품 교차매칭 차단(비범위 질의는 상품명 일치 필수), '주말' 등 흔한 단어 1개 매칭 차단, 슬라이딩(엔카/수수료) 분리, 네고 IRR/리스 가드.
- **판정형 답변**: NICE·KCB 점수 파싱 → 기준(tables.db 자동추출)과 대조 → 가능/불가 + 근거.

## 5. 프론트엔드
- 답변 길이 잘림 해결(`max_tokens`=ANSWER_MAX_TOKENS 기본 2048), 근거 청크 미리보기 확대.
- **처리과정 트레이스**(단계별 소요 ms·검색결과) 노출 + 웹 화면 3분할(채팅 / 처리과정 / 답변근거: 청크·graphify 노드).
- FastAPI 직접 서빙 채팅/대시보드(포트 8010), 예시칩·출처배지·피드백.

## 6. 프롬프트 / 템플릿 모듈화 (유지보수성)
- 흩어져 있던 답변 표현·정책과 프롬프트 지시문을 **중앙 모듈로 분리**(동작 불변, 65/65 유지).
- `app/prompts.py`: 시스템 프롬프트(상담사 톤·근거만·출처·모르면 모른다), 답변 스타일 4종, NO_EVIDENCE, 컨텍스트 정책(원문1차/구조화보조, 캡 MAX_CONTEXT_CHUNKS·CHARS, 저점수 컷 MIN_CONTEXT_SCORE), 어휘 관련성 게이트, build_messages.
- `app/templates.py`: 표기 정책 플래그(고객유형 미지정=내국인, 최저금리 산술 금지, 개월/출처 포맷), 문장 템플릿(rate_answer·nego_overview·promotion_criteria·promotion_judgment·condition_line·context_only·source_badge).
- `generator.py`/`tables_lookup.py`가 두 모듈만 참조 → 정책·톤 변경 시 두 파일만 수정, A/B 용이.

## 7. 문서 · 발표자료 정비
- `docs/` 현행화: ARCHITECTURE(조회 핸들러 순서·게이트웨이·저장소·UI·운영), PRD·TABLE_SCHEMA(실제 커버리지), QUERY_TRACE_EXAMPLE(처리 흐름 실측), PRODUCTION_TRANSITION(운영전환 2·3·5 구현 / 4·1·6 보류).
- `docs/PLAN_CHANGELOG.md`에 오늘 변경 이력 누적, 본 `WORKLOG` 작성.
- **presentation.html 전면 개편(17슬라이드)**: 중복 제거, 핵심 ①②③ 강조, 도식화 강화(아키텍처 SVG·파이프라인·실측 트레이스·3분할 데모 목업·판정 예시), prompts/templates 모듈 슬라이드 추가, 최신 수치 반영. 발표용 네비게이션(진행바·키보드·전체화면).

## 8. 검증 종합
- 골든셋 12 → **65문항**, eval **65/65(100%)**, 수치 100%, 출처 100%, pytest **28 통과**, CI 게이트 PASS.

## 남은 확인/과제
- NICE 885점의 등급(원문 2등급 vs 초기 예시 1등급) 도메인 확정.
- 사내망에서 `scripts/reindex.py` 재실행으로 신규 tables.db·실 임베딩 반영.
- 운영전환 보류분(인프라·SSO·보안)은 사내 사양 확정 후.
